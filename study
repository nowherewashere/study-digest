#!/usr/bin/env python3
"""Единая точка входа. Всё остальное — в studylib/, документация — в README.md."""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent))

from studylib.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
